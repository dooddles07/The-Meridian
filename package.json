{
  "name": "lumina-portal",
  "version": "1.0.0",
  "private": true,
  "description": "The Lumina — Community Living Portal. The Node server (./backend) serves the static frontend (./public) and the real API (JWT auth via httpOnly cookies, MongoDB) for resident and management login; other data stays on the client-side mock (public/js/client-backend.js).",
  "scripts": {
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^8.5.2",
    "express-validator": "^7.0.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^7.1.1",
    "mongoose": "^9.3.3",
    "morgan": "^1.10.0",
    "pdfkit": "^0.19.1"
  },
  "devDependencies": {
    "exceljs": "^4.4.0"
  }
}
